import { OnInit, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { CalendarMonthService } from './service/calendar-month.service';
import { ICalendarMonthConfig } from './config/calendar-month-config.model';
import { ICalendarDay, IDayEvent } from './config/day.model';
import { Moment } from 'moment';
export declare class DpCalendarMonthComponent implements OnInit, OnChanges {
    private calendarService;
    config: ICalendarMonthConfig;
    selected: Moment[];
    dayClick: EventEmitter<IDayEvent>;
    dayContextMenu: EventEmitter<IDayEvent>;
    weeks: ICalendarDay[][];
    weekdays: string[];
    constructor(calendarService: CalendarMonthService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    isDisabledDay(day: ICalendarDay): boolean;
}
