import { CalendarService } from '../dp-calendar/calendar.service';
import { EventEmitter } from '@angular/core';
import { IDayPickerConfig } from './day-picker-config.model';
import { UtilsService } from '../common/services/utils/utils.service';
import { ICalendarConfig } from '../dp-calendar/calendar-config.model';
export declare class DayPickerService {
    private calendarContainerService;
    private utilsService;
    readonly onPickerClosed: EventEmitter<null>;
    private defaultConfig;
    static isDateValid(date: string, format: string): boolean;
    constructor(calendarContainerService: CalendarService, utilsService: UtilsService);
    getConfig(config: IDayPickerConfig): ICalendarConfig;
    createValidator({minDate, maxDate}: {
        minDate: any;
        maxDate: any;
    }, dateFormat: string): (FormControl, string) => {
        [key: string]: any;
    };
    pickerClosed(): void;
}
