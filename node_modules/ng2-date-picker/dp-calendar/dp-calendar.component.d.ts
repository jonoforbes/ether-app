import { ICalendarMonthConfig } from '../dp-day-calendar/day-calendar-config.model';
import { IDayEvent } from '../dp-day-calendar/day.model';
import { ICalendarConfig } from './calendar-config.model';
import { CalendarService } from './calendar.service';
import { EventEmitter, OnChanges, SimpleChanges, OnInit } from '@angular/core';
import { Moment } from 'moment';
import { DayPickerService } from '../dp-day-picker/day-picker.service';
export declare class CalendarComponent implements OnInit, OnChanges {
    private dayPickerService;
    private calendarService;
    selected: Moment[];
    config: ICalendarConfig;
    theme: string;
    openOn: Moment[];
    themeClass: any;
    selectedChange: EventEmitter<Moment[]>;
    dayClick: EventEmitter<IDayEvent>;
    dayContextMenu: EventEmitter<IDayEvent>;
    calendarMove: EventEmitter<Moment>;
    calendars: ICalendarMonthConfig[];
    showMonthCalendar: boolean;
    constructor(dayPickerService: DayPickerService, calendarService: CalendarService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    daySelected({day, event}: IDayEvent): void;
    getMonthToDisplay(month: Moment): string;
    getYearToDisplay(year: Moment): string;
    monthSelected(index: number, month: Moment): void;
    moveCalendars(base: Moment, months: number): void;
    isLeftNavDisabled(month: Moment): boolean;
    isRightNavDisabled(month: Moment): boolean;
    shouldShowMonthSelector(index: number): boolean;
}
