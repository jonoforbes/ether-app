import { Moment } from 'moment';
import { UtilsService } from '../common/services/utils/utils.service';
import { IMonth } from './month.model';
import { ICalendarMonthConfig } from '../dp-day-calendar/day-calendar-config.model';
export declare class MonthCalendarService {
    private utilsService;
    constructor(utilsService: UtilsService);
    generateYear(year: Moment, selected?: Moment): IMonth[][];
    isDateDisabled(month: IMonth, config: ICalendarMonthConfig): boolean;
}
