import { OnInit, EventEmitter } from '@angular/core';
import { IMonth } from './month.model';
import { MonthCalendarService } from './month-calendar.service';
import { Moment } from 'moment';
import { ICalendarMonthConfig } from '../dp-day-calendar/day-calendar-config.model';
export declare class MonthCalendarComponent implements OnInit {
    private monthCalendarService;
    config: ICalendarMonthConfig;
    selected: Moment;
    monthClick: EventEmitter<Moment>;
    yearMonths: IMonth[][];
    constructor(monthCalendarService: MonthCalendarService);
    ngOnInit(): void;
    isDisabledMonth(month: IMonth): boolean;
    monthSelected(month: IMonth): void;
}
