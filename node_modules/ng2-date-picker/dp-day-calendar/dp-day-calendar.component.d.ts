import { OnInit, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { CalendarMonthService } from './day-calendar.service';
import { ICalendarMonthConfig } from './day-calendar-config.model';
import { ICalendarDay, IDayEvent } from './day.model';
import { Moment } from 'moment';
export declare class DayCalendarComponent implements OnInit, OnChanges {
    private calendarService;
    config: ICalendarMonthConfig;
    selected: Moment[];
    dayClick: EventEmitter<IDayEvent>;
    dayContextMenu: EventEmitter<IDayEvent>;
    weeks: ICalendarDay[][];
    weekdays: string[];
    constructor(calendarService: CalendarMonthService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    isDisabledDay(day: ICalendarDay): boolean;
}
