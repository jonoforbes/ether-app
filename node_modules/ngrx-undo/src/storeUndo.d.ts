import { Reducer, Dispatcher } from "@ngrx/store";
import { OpaqueToken } from "@angular/core";
export declare const UNDO_DEVTOOLS_CONFIG: OpaqueToken;
export declare const UNDO_ACTION: string;
export declare class StoreUndoModule {
    static interceptStore(options?: {
        bufferSize: number;
    }): {
        ngModule: typeof StoreUndoModule;
        providers: ({
            provide: OpaqueToken;
            useValue: {
                bufferSize: number;
            };
        } | {
            provide: typeof Reducer;
            deps: (OpaqueToken | typeof Dispatcher)[];
            useFactory: (dispatcher: Dispatcher, reducer: any, options: {
                bufferSize: 100;
            }) => Reducer;
        })[];
    };
}
